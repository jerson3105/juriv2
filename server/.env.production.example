# ===========================================
# CONFIGURACIÓN DE PRODUCCIÓN - JURIED
# ===========================================
# Copia este archivo como .env en el servidor de producción
# y completa los valores reales

# Base de datos MySQL (cPanel)
DATABASE_URL=mysql://usuario_db:contraseña_db@localhost:3306/nombre_db

# JWT Secret (genera uno seguro con: openssl rand -base64 64)
JWT_SECRET=tu_jwt_secret_muy_largo_y_seguro_aqui

# Entorno
NODE_ENV=production
PORT=3001

# URL del frontend (tu dominio)
FRONTEND_URL=https://plataformajuried.com
CORS_ORIGIN=https://plataformajuried.com,https://www.plataformajuried.com

# URL del backend (para construir URLs absolutas de archivos subidos)
BACKEND_URL=https://api.plataformajuried.com

# Configuración de archivos
UPLOAD_DIR=/home/tu_usuario/public_html/uploads
MAX_FILE_SIZE=5242880

# Email (opcional - para recuperación de contraseña)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@gmail.com
# SMTP_PASS=tu_app_password
